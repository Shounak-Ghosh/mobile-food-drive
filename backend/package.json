{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "main.py",
  "directories": {
    "test": "tests"
  },
  "scripts": {
  "start_linux": "source venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000",
  "dev_linux": "source venv/bin/activate && uvicorn main:app --reload",
  "start_windows": "python -m uvicorn main:app --host 0.0.0.0 --port 8000",
  "dev_windows": "venv\\Scripts\\python.exe -m uvicorn main:app --reload",
  "test": "pytest"
},
  "keywords": [],
  "author": "",
  "license": "ISC"
}

{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "main.py",
  "directories": {
    "test": "tests"
  },
  "scripts": {
  "start": "source venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000",
  "dev": "source venv/bin/activate && uvicorn main:app --reload",
  "_comment0": "python -m uvicorn main:app --host 0.0.0.0 --port 8000",
  "_comment1": "python -m uvicorn main:app --reload",
  "_comment2": "above works for BOTH WINDOWS AND MAC following instructions left in ReadMe",
  "test": "pytest"
},
  "keywords": [],
  "author": "",
  "license": "ISC"
}
